import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nova.ai.memory',
  appName: 'Nova AI Memory Companion',
  webDir: 'dist'
};

export default config;
